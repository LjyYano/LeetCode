这是一个 Github 项目，项目名称叫 LeetCode，详细且全面记录 LeetCode 题解。

# 输入规则
- 强制：在我输入一个以 https://leetcode.cn/problems/ 开头的链接时，请根据链接生成对应的 Java 代码文件和题解文件。
- 强制：在我输入 https://leetcode.cn/problemset 开头的链接时，根据 record.txt 文件，找到合适题目，然后生成对应的 Java 代码文件和题解文件。
- 强制：在我输 “继续 n 个” 入时，根据 record.txt 文件，找到合适题目，然后生成对应的 Java 代码文件和题解文件。
- 强制：不能主动删除 record.txt 文件，不能删除里面最大的题目数字编号，只能向里面增加新的题目编号。

# 项目文件结构说明
- 目录 src/main/java/ 存放 Java 题解代码，目录 题解/ 存放题解文件
- 强制：不需要暴力解法的代码，只需要最优解的代码
- 强制：在生成代码时，要参考 record.txt 文件，不要生成已经解决的题目。生成好后将对应的题目编号写入 record.txt 文件。
- 强制：有一些题目可能是会员题目打不开，此时不用生成对应的文件，但是要在 record.txt 文件中记录对应的题目编号，下次就跳过这些题目。

# Java 代码要求
- 强制：Java 文件格式参考模板文件：L0000_Template.java，严格参考这个模板文件的格式
- 强制：要有适当的注释，代码要有适当的换行和缩进，注重可读性
- 强制：文件最开始应该是 import 语句（如果有的话），之后是在类上面的注释，注释的格式要先是题目链接，之后是题目描述，要和参考模板文件保持一致
- Java 文件要能够编译运行，且能够通过 LeetCode 的测试用例
- Java class 的 main 方法运行几个典型的测试用例，要保证能够运行。
- Java class 的类名就使用文件名，例如 "L0001_TwoSum.java" 对应的类名就是 "L0001_TwoSum"
- 代码要简洁，思路清晰，注释要详细。强制：注释代码放在对应代码行的上方，而不是代码的右边。注释要使用中文。
- Java 文件名命名规则是 "Lxxxx_{description}.java"。例如 https://leetcode.cn/problems/two-sum/description/ 链接下 "L0001_TwoSum.java"，L 后面的数字固定为 4 位，且是对应的数字编号。
- 只能使用 JDK 1.8 版本的语法，不能引入第三方包
- src/main/java/common 是通用的工具类、数据结构等，可以被其他题目复用
- 要保证整个 java 文件可以正常运行，不要丢失对应的 import 语句。
- 强制：在生成好对应的 Java 代码文件后，运行 main 方法，验证输出符合预期。运行后需要删除生成的 .class 文件。

# 题解文件说明
- 强制：题解文件一定要严格遵循模板文件：L0000_模板.md，里面的二级标题、三级标题、四级标题名称和顺序不能改变（注意 emoji 也要保持一致）。题目描述下面的 **🔗 题目** 、**🏷️ 标签** 和 **🟢 难度** 要和模板文件保持一致。
- 强制：题解文件放在 题解/ 目录下，且文件名规则是 "Lxxxx_{chinese_description}.md"。例如 https://leetcode.cn/problems/two-sum/description/ 链接下 "题解/L0001_两数之和.md"，其“两数之和”中是题目的标题。
- 强制：题目描述要原样复制对应题目的描述，不要有任何修改，且要完整（包括所有示例、图片、提示等）
- 强制：解题思路要详细，给出解题步骤、分析过程，并针对一个典型示例逐步分析
- 强制：难度左边的 emoji 符号要根据标签内容变化，简单是 🟢、中等是 🟡、困难是 🔴
- 强制：中文和英文间要有一个空格，例如 "i=0时，补数=7" 要写成 "i=0 时，补数 =7"
- 强制：题解文件最开始的 date 要使用今天的日期，格式示例 "yyyy-MM-dd"。我发现你总是输错时间，请注意。提示：你可以使用 "date" 命令获取今天的日期。
- 强制：示例分析可以参考模板文件：L0000_模板.md，但是要根据题目实际情况进行调整，最好更加直观通俗易懂。可以使用多种形式分析（包括但不限于表格），符合人类的阅读习惯。
- 强制：公式要直观，例如不应该写成 n^2，而是写成 n²。
- 强制：每次输出完题解文件后，再次检查一下所有要求，包括但不限于
    - 文章 title 下面的 date，是否是实时获取的当前日期？
    - 示例分析是否足够直观清晰？
    - 公式是否直观？
    - 中文和英文间是否有一个空格？
    - 题目描述是否完整且原样复制对应题目的描述？
    - 解题思路是否详细？

<示例分析最佳实践>
在题解的示例分析部分，应遵循以下原则，选择最直观的方式展示算法过程：

1. 展示方式选择
   根据题目特点选择最合适的展示方式：
   - 数组/字符串处理：使用带颜色标记的数组展示，突出当前处理位置
   - 双指针/滑动窗口：使用 [左指针 ... 右指针] 形式标记范围
   - 动态规划：使用表格展示状态转移
   - 树/图结构：使用简单的 ASCII 图形
   - 回溯/递归：使用缩进展示递归层级
   - 数学问题：使用公式推导或图形化展示

2. 状态展示原则
   - 使用表格展示状态变化，包含所有关键信息
   - 每一步的变化要清晰可见
   - 使用 [] 标记当前处理范围
   - 使用颜色标记当前处理的元素
   - 使用箭头（→）表示状态转换
   - 关键节点使用 ✅ 标记完成状态

3. 步骤说明要求
   - 每个步骤配有简洁的文字说明
   - 说明包括：当前操作、原因、结果
   - 对特殊情况要单独说明
   - 步骤之间要有明显的递进关系

4. 格式规范
   数组处理示例：
   ```text
   原始数组：[2, 7, 11, 15]
   步骤 1：[2] 7, 11, 15    // 检查第一个元素
   步骤 2：[2, 7] 11, 15    // 处理第二个元素
   ```

   双指针示例：
   ```text
   数组：[1, 2, 3, 4, 5]
   步骤 1：[1] 2, 3, 4, [5]    // 左右指针初始位置
   步骤 2：1, [2] 3, [4], 5    // 指针移动一步
   ```

   状态转移示例：
   | 步骤 | 当前状态 | 操作 | 结果 | 说明 |
   |-----|---------|------|------|-----|
   | 1 | 初始状态 | 操作 A | 新状态 | 原因说明 |

5. 关键原则
   - 选择最适合题目特点的展示方式
   - 确保每一步的状态变化清晰可见
   - 突出展示关键节点和转换过程
   - 适当使用颜色和符号增强直观性
   - 配合简洁的文字说明
   - 表格、图形等要简洁易懂
   - 避免过于复杂的展示方式

6. 示例规模选择
   - 选择最小的、能说明问题的示例
   - 示例要能覆盖关键场景
   - 特殊情况要单独举例说明

7. 总结要点
   - 总结算法的核心思路
   - 说明关键的判断条件
   - 指出需要注意的边界情况
</示例分析最佳实践>

在生成完一道新的题目后，通过 git 提交到远程仓库，comment 为 "update {chinese_description}"。