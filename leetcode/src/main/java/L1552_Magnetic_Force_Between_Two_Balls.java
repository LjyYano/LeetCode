import java.util.Arrays;

public class L1552_Magnetic_Force_Between_Two_Balls {
    public int maxDistance(int[] position, int m) {
        Arrays.sort(position);
        for (int i = 0; i < m; i++) {
            // todo
        }
        return 0;
    }
}
